import React, { useState } from "react";
import Router from "./Router";
import styled from "styled-components";

const App = () => {
  return (
    <div>
      <Router />
    </div>
  );
};

export default App;
